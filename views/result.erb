<!DOCTYPE html>
<html>
	<head>
		<link rel="stylesheet" href="//netdna.bootstrapcdn.com/bootstrap/3.0.3/css/bootstrap.min.css">
	</head>
	<body>
		<p><a href="/">Zurück</a></p>
		<h1>
			Suchergebnis für "<%= @card %>":
		</h1>

		<% @result_hash.keys.each do |key| %>
			<p>
				<h3><a href=<%= '"' + @result_hash[key][0] + '"' %>><%= key.to_s %></a></h3>

				<% if @result_hash[key][1].nil? %>
	        		Nüschts
	        	<% else %>
	        		<% @result_hash[key].shift %>
					<table class="table table-striped">
				        <thead>
							<tr>
								<th>Karte</th>
								<th>Seltenh.</th>
								<th>Zustand</th>
								<th>Foil?</th>
								<th>Preis</th>
								<th>Verf.</th>
							</tr>
				        </thead>
				        <tbody>
				        	<% @result_hash[key].each do |row| %>
					        	<tr>
					        		<td>
					        			<a href=<%= '"' + "https://www.magickartenmarkt.de/" + row[0] + '"' %>>
					        				<%= row[1] %>
					        			</a>
					        		</td>
					            	<td><%= row[2] %></td>
					            	<td><%= row[3] %></td>
					            	<td><%= row[4] %></td>
					            	<td><%= row[5] %></td>
					            	<td><%= row[6] %></td>
					          	</tr>
					    	<% end %>    
				        </tbody>
			      	</table>
			    <% end %>
		    <p>
		<% end %>

		
	</body>
</html>